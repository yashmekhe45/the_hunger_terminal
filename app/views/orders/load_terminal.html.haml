.row
  .terminal-info-box
    .row
      = link_to order_path(1) do
        %div.right.material-icons.md-36 local_cafe
    .row
      %div.right
        = link_to "Today's Order", order_path(1)
    .row
      .col.s12.m6
        There is minimum order required to be placed with all the terminals Order will be confirmed if the bar turns all green

.row
  - @terminals.each do |terminal|
    .col.s6.m3
      .card.grey.lighten-4.terminal-box
        .card-content.white-text
          / %span.card-title
          .progress
            .determinate{:style => "width: #{terminal.confirmation_possibility}%"} 
            / above - 8 to adjust icon properly
            %i.right.material-icons.md-36.right{:style => "left: #{terminal.confirmation_possibility - 8}%"} restaurant_menu
            %i.amount.right ₹ #{terminal.min_order_amount.to_i}
          %p
            %h5.center-align.black-text= terminal.name
            .center-align
              .thumbnail
                = image_tag terminal.image_url(:thumb) if terminal.image?
                = image_tag "enjoy_food.png" unless terminal.image?
            .center-align
              = link_to "Order", new_terminal_order_path(terminal.id), class: "btn grey darken-4"
