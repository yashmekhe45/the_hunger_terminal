.indexcontainer
  %h3.text-center #{current_user.company.name.capitalize} Vendor's Today's Reports
  / = link_to 'last month', 
  .table-responsive
    %table.table-hover.table-bordered
      %tbody
        %tr
          %th.text-center #
          %th.text-center Name
          %th.text-center Total 
          %th.text-center MenuItems
          %th.text-center Quantity
          -terminal_name = nil
          -i=0
        - @terminals.each do |terminal|
          %tr  
          -unless terminal_name == terminal.name
            %td.text-center= i+1
            %td.text-center= terminal.name 
            -i = i+1
          -else
            %td.text-center
            %td.text-center  
          %td.text-center ₹ #{terminal.total}
          %td.text-center= terminal.menu_item_name
          %td.text-center= terminal.quantity
          -terminal_name = terminal.name

  .table-responsive
    %table.table-hover.table-bordered
      %tbody
        %tr
          %th.text-center #
          %th.text-center Name
          %th.text-center Total
        -@todays_terminals.each_with_index do |terminal,i|
          %tr
          %td.text-center= i+1
          %td.text-center= terminal.name
          %td.text-center= terminal.total