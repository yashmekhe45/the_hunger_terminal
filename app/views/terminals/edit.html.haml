.container
  .row
    .col-md-4
      %h2 Edit Terminal
      = simple_form_for @terminal, url: company_terminal_path(@terminal), html: { method: :patch, class: 'form-horizontal' } do |f|
        = render 'form', f: f
